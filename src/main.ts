import { createApp } from 'vue'
import App from './App.vue'

const div = document.createElement('div');
div.id = 'chrome-extension-ai-chat-2023-03-08-xl-main'
document.body.appendChild(div);

createApp(App).mount(div)